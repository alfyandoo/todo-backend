datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model todos {
  id          Int     @id @default(autoincrement())
  activity    String
  completed   Boolean
}
